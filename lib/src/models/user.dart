class User {
  String? id;
  String? username;
  String email;
  String password;

  // Constructor, with syntactic sugar for assignment to members.
  User(
    this.id,
    this.username,
    this.email,
    this.password,
  ) {
    // Initialization code goes here.
  }
}
